GRAWP001
REALNAME test2
IMAGE bcalarmclock.png
BYTECODE weapontest2.gs2bc
SCRIPT
findplayer("guest").addweapon(this);
//#CLIENTSIDE

function onWeaponFired() {
  setani("grab", nil);
  triggeraction(getFrontPos()[0], getFrontPos()[1], "Test", 1);
}

public function getFrontPos(dir) { 
  return ({{player.x + .5, player.y - 1},{player.x - 1, player.y + 1},{player.x + .5, player.y + 3.25},{player.x + 3.05, player.y + 1}})[(dir? dir : player.dir)];
}
SCRIPTEND
